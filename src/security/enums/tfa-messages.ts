export enum TFAMessages {
  FOUND = 'Two factor auth has been found.',
  ALREADY_ENABLED = 'Two factor auth is already enabled.',
  NOT_ENABLED = 'Two factor auth is already enabled.',
  TFA_ENABLED = 'Two factor auth has been enabled.',
  TFA_DISABLED = 'Two factor auth has been disabled.',
}
